# Generate with: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=replace-with-a-long-random-token

# OpenClaw model identifier and its provider credential.
OPENCLAW_MODEL=openai/gpt-5.4
OPENAI_API_KEY=

# Loopback-only host port. Change this for concurrent cloned brains.
OPENCLAW_GATEWAY_PORT=18789
